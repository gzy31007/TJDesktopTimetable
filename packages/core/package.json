{
  "name": "@tjt/core",
  "version": "0.1.0",
  "private": true,
  "description": "课表核心：统一模型、周次/冲突/布局算法、多校适配器（纯 TypeScript，零平台依赖）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./adapters": "./src/adapters/registry.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0"
  }
}
